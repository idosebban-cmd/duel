/**
 * Feature flags — flip to `false` to instantly disable a feature without redeployment.
 */

/** Enables the "Just Play" intent flow: play-only matching, intent badges, post-match routing. */
export const ENABLE_JUST_PLAY = true;
